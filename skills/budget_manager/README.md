# Budget Manager Skill V1

Budget Manager is a guarded workflow for Meta budget adjustment.

Default is always `NO_CHANGE`.

Use:

```bash
python main.py --budget-manager-preview
```

Do not run apply until the preview has been reviewed.
